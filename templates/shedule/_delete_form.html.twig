<form method="post" action="{{ path('app_shedule_delete', {'id': shedule.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ shedule.id) }}">
    <button class="btn">Delete</button>
</form>
